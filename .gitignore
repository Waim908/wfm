/obj
/libcdio
*.exe
*.exe.dat
*.zip
wine/
wine-10.10-custom/
wfm-vanilla/
package-lock.json
readme.txt
.cflags
.cflags.*
.variant

# 本地工作区数据（记忆/技能等），不入版本库
.workbuddy/
.zcode/

# 代码审查报告，仅本地留存
CODE_REVIEW_PERF_SECURITY.md

# 回归审计报告，仅本地留存
REGRESSION_AUDIT_2026-09-12.md

# 主机侧对照预览脚本：只在本地出 PNG 到 .workbuddy/，不参与构建，不入版本库
tools/preview_*.py

# 注：res/*.svg 是入库的素材源文件，不忽略 —— Tango 图标集无需授权与协议限制，
# 可光明正大提交（来源与产物对应关系见 res/about-tango.txt）。
# 它只被 tools/gen_shortcut_overlay.sh 之类的产线脚本读取，不参与构建；
# res/ 里真正编进 exe 的仍是同名 .ico。
