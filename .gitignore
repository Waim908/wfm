/obj
/libcdio
*.exe
*.exe.dat
*.zip
wine/
wine-10.10-custom/
wfm-vanilla/
package-lock.json
readme.txt
.cflags
.cflags.*
.variant

# 本地工作区数据（记忆/技能等），不入版本库
.workbuddy/
.zcode/

# 代码审查报告，仅本地留存
CODE_REVIEW_PERF_SECURITY.md

# 回归审计报告，仅本地留存
REGRESSION_AUDIT_2026-09-12.md

# 主机侧对照预览脚本：只在本地出 PNG 到 .workbuddy/，不参与构建，不入版本库
tools/preview_*.py

# 角标产线的 SVG 源素材：只被 tools/gen_shortcut_overlay.sh 读取，不参与构建，
# 不入版本库（res/ 里只留会编进 exe 的东西）。字节级的产物 res/shortcut_overlay.ico
# 已在版本库中，本文件留着只为「以后还想重跑产线换配色」。
res/*.svg
